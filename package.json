{
  "name": "encoach-website",
  "version": "0.1.0",
  "private": true,
  "description": "Encoach.ai institutional website — Hugo + Tailwind v4",
  "scripts": {
    "dev": "hugo server --buildDrafts --disableFastRender",
    "build": "hugo --gc --minify",
    "clean": "rimraf public resources"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@tailwindcss/cli": "^4.0.0"
  }
}
